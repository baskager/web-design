<header class="project" style="background: url({{img}}); background-position: center;">
    <div class="wrap">
        <div class="container grid">
            <div>
                <h1 class="offsetBackground">{{title}}</h1>
                <p>
                    {{{summary}}}
                </p>
            </div>
        </div>
    </div>
</header>